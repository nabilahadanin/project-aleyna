import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App'; // jangan lupa huruf besar A-nya

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);